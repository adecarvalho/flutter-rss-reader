import 'package:flutter/material.dart';

class AddFluxPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ajouter un flux'),
      ),
      body: Center(
        child: Text('Add flux'),
      ),
    );
  }
}
